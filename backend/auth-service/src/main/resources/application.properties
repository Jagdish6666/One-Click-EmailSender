server.port=8082
spring.application.name=auth-service

# Clerk Authentication (Using Placeholder to avoid startup crash, override via env variable)
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${CLERK_JWKS_URI:https://clerk.example.com/.well-known/jwks.json}

app.cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://localhost:3002
